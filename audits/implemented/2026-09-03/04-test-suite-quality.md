# Test Suite Quality Audit — pyutilz (2026-09-03)

## Summary

Read `CLAUDE.md`, `audits/implemented/2026-09-02/04-test-suite-quality.md` (all 21 findings, most COMPLETED), `TESTING.md`, `tests/test_meta/test_code_audit_tests_baseline.py` and its reviewed baseline `tests/test_meta/_code_audit_tests_baseline.json`, and `tests/test_meta/test_test_source_parity.py`.

Verified still-fixed from the 2026-09-02 wave, not re-raised: F08 (`--cov-fail-under=82` at `.github/workflows/ci.yml:91` now matches `fail_under = 82` at `pyproject.toml:458`), F10 (no unconditional `@pytest.mark.skip` remains anywhere under `tests/` — all 20 skip/xfail marks are `skipif` with a platform/dependency predicate), F12 (`filterwarnings` policy live in `tests/conftest.py`), F16 (all 8 remaining fixtures that no test names explicitly are `autouse=True`; zero dead fixtures repo-wide), F17 (`-m slow` collects 42/4258 tests), F21 (TESTING.md no longer pins counts and documents `--no-cov` on Windows). Zero duplicate test-function names per class/module.

Ran on this box (`PY_CI_SHARED_DIR` exported, `python -m pytest --no-cov -q -p no:randomly`): `tests/test_meta/test_test_source_parity.py` (2 passed), `tests/test_meta/test_code_audit_tests_baseline.py tests/test_security_audit_20260902.py tests/test_llm_deepseek.py tests/test_pandaslib_extra.py` (129 passed, 2 skipped), `tests/test_system_audit_fixes.py tests/test_meta/test_optional_deps_isolation.py` (55 passed), and `--collect-only -m slow` (42/4258).

Static analysis: executed the repo's own scanners from `pyutilz.dev.code_audit` directly against `tests/` (`nondiscriminating_test`, `source_text_assertion`, `vacuous_assertion`, `except_skip_masks_call_under_test`, `hardcoded_absolute_path_in_test`, `vacuous_empty_pattern_match`, `stale_test_spy_arity`, `tautological_is_not_none_only_test`, `patch_target_is_a_reexport`, `lazy_log_assertion`, `test_asserts_against_production_constant`, `unenforced_docstring_invariant`); plus AST scans for assertion-free tests, all-assertions-under-a-guard tests, duplicate test names, never-requested fixtures, unrestored `sys.modules` / `os.environ` writes, wall-clock threshold assertions, and public-function-vs-test-corpus coverage (561 public functions under `src/pyutilz/`, 18 never named anywhere in `tests/` after excluding the coverage-omitted trees). Per-scanner positive/negative discipline was checked for all 89 registered scanners.

All 13 entries in `_code_audit_tests_baseline.json` were re-read against their sources; every one is still a justified false positive or accepted precondition — only its documentation has drifted (F10 below).

Findings: 2 High, 5 Medium, 5 Low (12 total).

## Findings

### F01. [High] The two scanners written specifically to catch tests that cannot fail are not in the tests/ check set — 74 findings sit unbaselined and undrained — tests/test_meta/test_code_audit_tests_baseline.py:59
- **Disposition**: COMPLETED — `nondiscriminating_test` wired into `_TEST_QUALITY_CHECKS` at tests/test_meta/test_code_audit_tests_baseline.py:104; re-measured at 66 (not 74) after the wave, of which 60 were DRAINED by adding the assertion each test's name promised (34 files -- e.g. tests/test_similarity.py:371ff now assert the refusal verdict `(nb is None) == (py is None)`, tests/test_web.py:61 asserts all six globals reset plus the replaced session being closed, tests/test_system.py:186 turned out to be permanently vacuous and now passes `return_usage_stats=True`), and 6 baselined as reviewed false positives (five deliberately advisory meta-tests that each have a blocking sibling, plus tests/test_monitoring.py:105, a nested helper matching the `test_` prefix by accident). Per-entry justifications are written into the module docstring at tests/test_meta/test_code_audit_tests_baseline.py:22-79, which now also records that a new test-quality scanner must be added to the literal in the same change that registers it.
- **Category**: unenforced-self-tooling
- **Problem**: `_TEST_QUALITY_CHECKS` (lines 59-67) lists seven checks. `nondiscriminating_test` is not among them, although it is registered in `get_scanners()`, is **not** in `OPT_IN_ONLY`, and exists solely to flag test code (commit d7123b8, "code_audit: three checks for tests that cannot fail"). Running `get_scanners()["nondiscriminating_test"](Path("tests"))` on this box returns **74 findings** — 44 `no-assert`, 22 `all-asserts-conditional`, 8 `swallows-assertionerror` — across 45 files, none of them in `_code_audit_tests_baseline.json`. `tests/test_meta/test_code_audit_tests_baseline.py` passes green today with all 74 present (verified in the 129-passed run above). Representative sites, each verified by reading the body: `tests/test_llm_deepseek.py:136`, `tests/test_llm_openrouter.py:1338`, `tests/test_web.py:61`, `tests/test_logginglib.py:182`, `tests/test_logginglib.py:189`, `tests/test_pandaslib_extra.py:81`, `tests/test_db_extra.py:683`, `tests/test_llm_domain_extra.py:158`, `tests/test_architecture_audit_20260902.py:94`, `tests/test_disk_cache_audit_regression.py:42`, `tests/test_gpu_occupancy.py:110`, `tests/test_llm_openai_compat.py:338`, `tests/test_monitoring.py:216`, `tests/test_system.py:186`, `tests/test_system_extra3.py:64`, `tests/test_strings.py:141`, `tests/test_smoke_untested_modules.py:65`. The exclusion is undocumented: the module docstring explains why `redundant_test_fit_call`, `duplicate_function_body` and the checks with their own meta-tests are out of scope, and says nothing about `nondiscriminating_test`.
- **Failure scenario**: this is 2026-09-02 F01 recurring for the scanners added since. The wave's fix wired a fixed seven-check list rather than "every check that targets test code", so each new test-quality scanner lands unwired by default and its findings accumulate silently. Concretely, `tests/test_llm_openrouter.py:1338` and `tests/test_llm_deepseek.py:136` construct a 402 response, call `_handle_special_status`, and assert nothing — a change that deletes the credit-exhaustion warning entirely leaves both green and indistinguishable from their `200` no-op siblings (see F05).
- **Suggested fix**: add `nondiscriminating_test` to `_TEST_QUALITY_CHECKS`, refresh the baseline with the same per-entry review the existing 13 entries received, and drain the ones that are genuinely defective (F05, F07, F08, F09 below are drawn from this list) rather than baselining wholesale. Longer term, derive the check set from a registry attribute ("this scanner targets test code") instead of a hand-maintained literal, so the next scanner of this class is wired by construction.

### F02. [High] `source_text_assertion` is not wired at tests/, and the getsource string-matching class the 2026-09-02 wave eliminated has regrown — 7 live sites — tests/test_security_audit_20260902.py:56
- **Disposition**: COMPLETED — `source_text_assertion` wired at tests/test_meta/test_code_audit_tests_baseline.py:105; all 8 live sites (7 in tests/test_security_audit_20260902.py plus tests/test_domain_core_dev_system_audit_20260903.py:567 -- one more than the report's 7) converted to behavioural assertions: the CLI/SDK flags are asserted on the argv handed to a patched `subprocess.Popen` and on the `ClaudeCodeOptions.extra_args` handed to a stubbed `cc_query`, the tool-use guard drives `_generate_sdk` with a stubbed stream and asserts `ClaudeCodeToolUseError` is raised, and the prefect-key site asserts the secret never reaches `caplog.text` while still reaching `client.api_key`. The scanner now reports 0 findings under tests/ and the baseline holds no `source_text_assertion` entry.
- **Category**: source-inspection-instead-of-behavioral
- **Problem**: 2026-09-02 F02 was dispositioned COMPLETED ("all ten `inspect.getsource()` string assertions are gone"). That holds for the four files it named, but `tests/test_security_audit_20260902.py` — added in the same wave — carries seven new ones, verified by running `get_scanners()["source_text_assertion"](Path("tests"))`, which reports exactly these and nothing else: lines **56, 64, 80, 82, 244, 251, 252**. Read in place: `:56` asserts `"'--strict-mcp-config'" in src` over `getsource(ClaudeCodeProvider._generate_cli)`; `:64` asserts `'"strict-mcp-config": None' in src`; `:80`/`:82` assert `"raise ClaudeCodeToolUseError" in src` and that a specific old `logger.warning(...)` literal is absent; `:244` asserts `"os.O_EXCL, 0o600" in src`; `:251`/`:252` assert `"0o600" in inspect.getsource(safe_pickle)` and the same over `disk_cache.DiskCache.put`. `source_text_assertion` is registered, is **not** `OPT_IN_ONLY`, and was added for precisely this defect class (commit f68bc59) — but `_TEST_QUALITY_CHECKS` does not list it, so the tests/ meta-test never sees these.
- **Failure scenario**: these are the regression guards for four security fixes (ambient-MCP isolation, tool-use escape, owner-only file modes). Renaming the local variable holding the argv, or moving the flag into a shared constant, fails the test with the security property intact; conversely `argv.append("--strict-mcp-config")` inside a branch that never executes, or a `0o600` that only appears in a comment, passes it with the property gone. The scanner's own wording — "it passes whether or not the inspected code runs" — applies unchanged.
- **Suggested fix**: add `source_text_assertion` to `_TEST_QUALITY_CHECKS` (the same one-line wiring gap as F01), then replace each site behaviourally: patch `subprocess` / the SDK entry point and assert the flag appears in the argv or `extra_args` actually passed; drive `_generate_sdk` with a stubbed stream that yields a tool-use block and assert `ClaudeCodeToolUseError` is raised; assert the resulting file mode for `safe_pickle` and `DiskCache.put` the way `test_atomic_write_bytes_result_is_owner_only` (line 254) already does for `atomic_write_bytes`.

### F03. [Medium] The module/test parity gate waves through any module under a subpackage that has a namesake test file — `system/psutil_compat.py` has zero test references — tests/test_meta/test_test_source_parity.py:335
- **Disposition**: COMPLETED — the bare `f"test_{sub}"` fallback in `_candidate_test_names` (tests/test_meta/test_test_source_parity.py) now applies only when the module stem equals the subpackage name, and `_TEST_EXEMPT_MODULES` accepts path-relative keys so generic stems (`base`, `memory`, `session`, `misc`) cannot blanket-waive unrelated modules. Re-measured: 16 modules were masked, not 19, and exactly one was wholly untested -- `system/psutil_compat.py`, which now has tests/test_psutil_compat.py (18 behavioural tests simulating other platforms via `monkeypatch.delattr(psutil, ...)` as its own docstring prescribes, pinning that `get_cpu_freq_current_mhz` returns None and not 0.0 when unavailable and that a genuine 0.0 reading stays distinguishable from None). `prefect.get_schema` gained two real tests; the other 14 modules are path-keyed exemptions each citing the specific covering test file.
- **Category**: untested-public-api
- **Problem**: `_candidate_test_names` appends `f"test_{sub}"` (line 335), where `sub` is the first path component. `tests/test_system.py`, `tests/test_web.py` and `tests/test_cloud.py` exist, so **every** module under `src/pyutilz/system/`, `src/pyutilz/web/` and `src/pyutilz/cloud/` satisfies the gate regardless of its own name. Computed on this box, 19 modules pass only through that fallback. The live miss is `src/pyutilz/system/psutil_compat.py`: its four public functions `has_psutil_function` (:47), `missing_psutil_functions` (:57), `get_cpu_freq` (:76), `get_cpu_freq_current_mhz` (:96) are named nowhere under `tests/` (word-boundary grep over every `tests/**/*.py`), there is no `tests/test_psutil_compat.py`, and the module is not in `_TEST_EXEMPT_MODULES`. `test_every_production_module_has_a_test_file` passes (verified: 2 passed). `has_psutil_function`'s docstring even states "tests simulate other platforms by deleting attributes from the psutil module" — no such test exists.
- **Failure scenario**: the gate's stated purpose ("catches the failure mode where a new module is added to the package without a test file") does not hold for three subpackages. `psutil_compat` is a graceful-degradation shim whose entire contract is returning `None` instead of raising on platforms lacking `cpu_freq`; `get_cpu_freq_current_mhz` returning `0.0` instead of `None` — the corruption its own docstring warns about — has nothing to catch it.
- **Suggested fix**: drop the bare `f"test_{sub}"` candidate, or restrict it to a module whose stem equals the subpackage name (`system/system.py`), then disposition the 19 modules it was masking — add `tests/test_psutil_compat.py` first, and record the rest as exemptions with a cited covering file. The 18 untested public functions the same scan found are the wider list: `src/pyutilz/data/polarslib/_common.py:57,68`; `src/pyutilz/database/db/connection.py:33,181,211`; `src/pyutilz/database/db/execution.py:323,462`; `src/pyutilz/database/db/regjobs.py:13`; `src/pyutilz/database/db/schema.py:134,250,270,281,304`; `src/pyutilz/performance/kernel_tuning/__init__.py:20` (`array_location`); plus the four `psutil_compat` entries.

### F04. [Medium] "positive+negative case per scanner" is the stated basis for ~50 test-file exemptions, nothing enforces it, and it is false for two scanners — tests/test_meta/test_test_source_parity.py:74
- **Disposition**: COMPLETED — new meta-test tests/test_meta/test_scanner_positive_and_negative_cases.py enforces, for all 95 registered scanners (not 89), that each has a test asserting a non-empty result and one asserting an empty result, reading polarity statically per scanner and unwrapping `functools.partial`; `_NO_CLEAN_CASE_POSSIBLE` is empty and a third test fails on a stale exemption. Re-measured against the current tree: `comment_cites_absolute_line` is no longer missing its negative (the wave added one), leaving exactly one real gap -- `partial_guard_across_siblings` -- now closed by a negative in tests/code_audit/test_partial_fix.py whose three siblings guard via three different spellings, so silence cannot be a template artefact. The ~50 `_TEST_EXEMPT_MODULES` justification strings now name their actual covering file under tests/code_audit/. Its `_polarity` AST reader tripped the C901 ratchet on first full-suite run and was split behaviour-preservingly into `_result_names` / `_compared_polarity` / `_bare_polarity` (tests/test_meta/test_scanner_positive_and_negative_cases.py:83-150).
- **Category**: unenforced-docstring-invariant
- **Problem**: The comment at lines 73-74 — "every scanner submodule is exercised jointly by tests/test_code_audit.py (positive+negative case per scanner)" — is the justification attached to roughly fifty `_TEST_EXEMPT_MODULES` entries reading "code_audit scanner; covered by test_code_audit.py". No meta-test checks it. Enumerating all 89 registered scanners and every test that calls them, two have a positive (flags-the-defect) test and no negative (clean-input-is-silent) test: `comment_cites_absolute_line` — only `tests/test_code_audit.py:7293` `test_comment_cites_absolute_line_finds_a_citation`, which asserts `len(findings) == 1` for a comment containing "line 619", with nothing asserting that a version number, an issue number or a bare figure is *not* read as a line citation; and `partial_guard_across_siblings` — only `tests/test_code_audit.py:5099`, with no case where every sibling carries the guard and the scanner must stay silent. (`inconsistent_filter` looked like a third but its single test at `tests/test_code_audit.py:5120` covers both directions.)
- **Failure scenario**: `comment_cites_absolute_line` is documented at `tests/test_code_audit.py:7285` as producing "225 hits in one package, most of them legitimate" — a false-positive-heavy rule with no false-positive test. A narrowing that accidentally disables the rule, or a widening that makes it match every number in every comment, is caught by neither. More broadly, the exemption text asserts a discipline that new scanners can silently fail to meet, which is how these two arrived.
- **Suggested fix**: add a meta-test that, for every name in `get_scanners()`, requires at least one test asserting a non-empty result and one asserting an empty result over that scanner's callable — the same shape as the existing `test_meta` baselines, with a reviewed exemption dict for any scanner that genuinely cannot have a clean case. Then add the two missing negatives.

### F05. [Medium] The `_handle_special_status` "warns" tests are indistinguishable from their no-op siblings — three files — tests/test_llm_deepseek.py:136
- **Disposition**: COMPLETED — all five sites now discriminate: tests/test_llm_deepseek.py:138 and tests/test_llm_openrouter.py:1341 assert exactly one WARNING naming the status and the balance/credits condition, their 200 siblings (tests/test_llm_deepseek.py:153, tests/test_llm_openrouter.py:1356) assert `caplog.records == []`, and the base-class no-op at tests/test_llm_openai_compat.py:414 asserts the hook returns None and emits nothing.
- **Category**: assertion-free-coverage-chasing
- **Problem**: `test_handle_special_status_402_warns` (`tests/test_llm_deepseek.py:136`) builds an `httpx.Response(status_code=402)`, calls `p._handle_special_status(resp)`, and ends — no `caplog`, no patched logger, no `pytest.warns`. Its sibling `test_handle_special_status_200_noop` (`tests/test_llm_deepseek.py:145`) is identical except for the status code and likewise asserts nothing, so the pair cannot distinguish the warn path from the no-op path. The identical pair exists at `tests/test_llm_openrouter.py:1338` (`test_402_warns`, whose comment says "Must not raise — warn-only behaviour") and `tests/test_llm_openrouter.py:1347` (`test_200_no_op`), and the no-op half alone at `tests/test_llm_openai_compat.py:393`. All five are in the F01 `no-assert` list.
- **Failure scenario**: 402 is credit exhaustion. A regression that drops the warning — or emits it on 200 instead — passes all five tests; the operator's only signal that a paid provider has run out of credit disappears silently while the retry loop keeps spinning against a dead key.
- **Suggested fix**: assert on `caplog` in the 402 cases (the warning must fire and must name the status or the provider) and assert no records were emitted in the 200 cases, so the two halves of each pair actually differ.

### F06. [Medium] `tests/test_code_audit.py` is 8678 lines; the repo's own 1k-LOC module-size gate scans `src/` only — tests/test_meta/test_shared_checks_wired.py:58
- **Disposition**: COMPLETED — budget EXTENDED to tests/, and the file split. The decision: exempting tests/ would have left the repo's single largest file (11306 lines by then, 8.7x the limit) growing one block per scanner while the production side of the same feature had been split for that exact rule. tests/test_code_audit.py is now tests/code_audit/ -- 89 files, 12091 lines, largest 492, mirroring src/pyutilz/dev/code_audit/ one file per scanner family plus _helpers.py for the four genuinely shared definitions; code moved verbatim, and the collected test-name sets before and after differ by exactly the one intentional F04 addition (705 -> 706, all passing). `test_no_new_file_over_1k_loc` (tests/test_meta/test_shared_checks_wired.py) now feeds `_production_py_files() + _test_py_files()` with `growth_slack=0`, grandfathering the four files over 1k at their measured current size (test_llm_openrouter.py 1991, test_domain_db_web_cloud_llm_text_audit_20260902.py 1251, test_domain_core_dev_system_audit_20260903.py 1184 -- which the report missed -- and test_web_extra.py 1064) so they can shrink but never grow. TESTING.md documents both.
- **Category**: test-organization
- **Problem**: `_production_py_files()` returns `sorted((REPO_ROOT / "src").rglob("*.py"))` with the docstring "src/ only, so tests/benchmarks/scripts are out of scope", and `test_no_new_file_over_1k_loc` (line 62) enforces CLAUDE.md's module-size rule against that list only. `tests/test_code_audit.py` is 8678 lines — 8.7x the documented limit and the largest file in the repo by a factor of four (next: `tests/test_llm_openrouter.py` 1991, `tests/test_domain_db_web_cloud_llm_text_audit_20260902.py` 1248, `tests/test_web_extra.py` 1061). It absorbed roughly 2400 lines over the ~40 commits since the 2026-09-02 wave, one block per new scanner, while the production side of the same feature was deliberately split into the `src/pyutilz/dev/code_audit/` subpackage of one module per scanner precisely to respect that rule.
- **Failure scenario**: the file is the single place where per-scanner coverage is asserted, and at this size nobody reads it end to end — which is how F04's two missing negatives went unnoticed and why the per-scanner clusters have drifted into inconsistent shapes. Four other test files are already over or near 1000 lines with no gate to notice the next one.
- **Suggested fix**: split `tests/test_code_audit.py` along the same seams as `src/pyutilz/dev/code_audit/` (e.g. `tests/code_audit/test_<family>.py`), mirroring the production split, and extend the LOC gate to `tests/` with a reviewed baseline so the existing large files are grandfathered and no new one is added.

### F07. [Medium] `test_connect_to_db_close_failure_does_not_prevent_retry` proves nothing about the retry it is named for — tests/test_db_extra.py:683
- **Disposition**: COMPLETED — tests/test_db_extra.py now asserts `attempts["n"] == 2` and that the installed `db.conn` is the `WorkingConn`, matching the sibling above it, so a regression that swallows the close() failure and returns without retrying no longer passes. The two weaker siblings named alongside it (the make_set_excluded_clause and ensure_pg_table_exists "is the real implementation" pair) were drained too: they now assert the returned clause text and that the snake_case entry point really ran the existence probe.
- **Category**: assertion-free-coverage-chasing
- **Problem**: The test builds a `BrokenCloseConn` whose `set_isolation_level` and `close` both raise, counts attempts in `attempts["n"]` via `fake_psycopg2_connect`, calls `db.connect_to_db(...)` — and never asserts anything, including the `attempts["n"]` counter it goes to the trouble of maintaining. Its immediate predecessor (ending at `tests/test_db_extra.py:679`) asserts `closed["n"] == 2` and `attempts["n"] == 3` on the same harness, so the omission is an inconsistency within the file rather than a house style. Related, weaker cases in the same file: `tests/test_db_extra.py:151` and `tests/test_db_extra.py:858` encode a real contract ("must not raise under `simplefilter('error', DeprecationWarning)`") but express it only as the absence of an exception.
- **Failure scenario**: the docstring's contract is "the retry loop must still continue". A regression where `connect_to_db` swallows the `close()` failure *and* returns `None` without retrying satisfies the test exactly — no exception propagates — and the caller receives a `None` connection instead of a working one.
- **Suggested fix**: assert `attempts["n"] == 2` and that the returned connection is the `WorkingConn` instance, matching the sibling test above it.

### F08. [Low] `test_max_vars_positive` requests `capsys` and never reads it, alone among its siblings — tests/test_pandaslib_extra.py:81
- **Disposition**: COMPLETED — tests/test_pandaslib_extra.py:81 now reads `capsys` and asserts exactly one value line survived the cap and that it is the most frequent value, so a `max_vars` that is ignored or inverted fails.
- **Category**: fixture-misuse
- **Problem**: `test_max_vars_positive(self, capsys)` calls `showcase_df_columns(df, max_vars=1, use_markdown=False, use_print=True)` and ends with the comment `# Should not raise`. The `capsys` fixture is requested and never used. Both neighbours — `test_max_vars_zero` (`tests/test_pandaslib_extra.py:75`) and `test_dropna` (`tests/test_pandaslib_extra.py:87`) — take `capsys` and assert on `captured.out`. `max_vars=1` is the one parameter under test and nothing observes its effect.
- **Failure scenario**: `max_vars` being ignored (all columns shown) or inverted (none shown) passes. The requested-but-unread fixture also reads, to a later contributor, as though output *is* being checked.
- **Suggested fix**: call `captured = capsys.readouterr()` and assert exactly one variable block is present, i.e. that the cap took effect.

### F09. [Low] `test_init_logging_file` asserts nothing about the file or the handler it exists to verify — tests/test_logginglib.py:189
- **Disposition**: COMPLETED — tests/test_logginglib.py:228 asserts exactly one `FileHandler` whose `baseFilename` is the path the contract actually derives -- `_log_filename(forced_filename)`, the trailing `.py` swapped for `.log`, NOT `forced_filename` itself as the report assumed -- then emits a record and asserts it lands in that file; the console sibling asserts a stream handler and no file handler. Both sit under a new autouse fixture that restores the root logger, since `init_logging` calls `basicConfig(force=True)` process-wide.
- **Category**: assertion-free-coverage-chasing
- **Problem**: `test_init_logging_file(self, tmp_path)` builds `log_file = tmp_path / "test.log"`, calls `init_logging(log_to_file=True, forced_filename=str(log_file), level="DEBUG")`, and ends — it never checks that the file was created, that a `FileHandler` was attached, or that the level took; `log_file` is computed and then unused beyond being passed in. Its sibling `test_init_logging_console` (`tests/test_logginglib.py:182`) is the `log_to_file=False` case and likewise asserts nothing, so the pair cannot distinguish the two modes. Both are in the F01 `no-assert` list.
- **Failure scenario**: `forced_filename` being ignored (logging to the derived default path instead), or the file handler never being attached, passes both tests — a caller's log lines go somewhere else, or nowhere, with the suite green.
- **Suggested fix**: after the call, assert a `logging.FileHandler` whose `baseFilename` resolves to `log_file` is attached, emit one record and assert it lands in the file; assert no `FileHandler` is added in the console case.

### F10. [Low] The tests baseline's per-entry review notes have drifted from the baseline they document — tests/test_meta/test_code_audit_tests_baseline.py:32
- **Disposition**: COMPLETED — review notes rewritten at tests/test_meta/test_code_audit_tests_baseline.py:22-79: counts corrected (`except_skip_masks_call_under_test` x5, including the previously undocumented test_meta/test_complexity_ratchet.py entry, and `vacuous_empty_pattern_match` x7), and the notes are now keyed by check plus TEST FUNCTION NAME rather than by absolute line number, which is the rot the finding identified.
- **Category**: docs
- **Problem**: The module docstring enumerates the reviewed baseline entries. Three mismatches against `tests/test_meta/_code_audit_tests_baseline.json` as it stands: the docstring says `except_skip_masks_call_under_test` **x4** and names `test_pandaslib_extra2.py:248` and `test_strings_extra2.py:502`, while the baseline holds **5** such entries and records `test_pandaslib_extra2.py:243` and `test_strings_extra2.py:500`; and the fifth entry, `except_skip_masks_call_under_test::test_meta/test_complexity_ratchet.py:47`, is not mentioned anywhere in the review notes. I re-read all three sites — `tests/test_pandaslib_extra2.py:243` narrows to the `pd.array(...)` construction, `tests/test_strings_extra2.py:500` to `spacy.load()`, and `tests/test_meta/test_complexity_ratchet.py:47` skips when ruff is absent or returns non-JSON — so all three are genuine false positives and this is a documentation defect only, not a masked bug.
- **Failure scenario**: the docstring is the sole record of *why* each entry was accepted, and the refresh instruction ("after REVIEWING each new entry") depends on it being complete. An entry that arrived without review is now indistinguishable from one that was reviewed, which is exactly what happened to the `test_complexity_ratchet.py` line. The absolute line numbers will keep rotting — the repo ships a `comment_cites_absolute_line` scanner for this class, unwired here.
- **Suggested fix**: refresh the counts and line numbers and add the `test_complexity_ratchet.py:47` justification; better, key the notes by `check::file` plus the test-function name rather than by line, so an edit above the site does not silently invalidate them.

### F11. [Low] A stub `prefect` module is installed in `sys.modules` for the whole session and never removed — tests/test_system_audit_fixes.py:321
- **Disposition**: COMPLETED — the bare `sys.modules["prefect"] = ...` write is gone: tests/test_system_audit_fixes.py:316 is now a `prefect_helpers` fixture whose finally block removes the stub key and, when the fixture built it, `pyutilz.system.scheduling.prefect` plus the parent-package attribute. The blind spot is closed too -- tests/test_meta/test_no_module_reload.py gained `test_no_unreviewed_sys_modules_assignment` (AST scan for `sys.modules[...] = ...` under tests/, flagging ANY module name, since the hazard is precisely a stub shadowing a MISSING third-party package) plus a stale-entry test, with six reviewed whitelist entries; verified it bites by watching it flag the prefect site before the fix.
- **Category**: order-dependence
- **Problem**: `_prefect_module()` does `sys.modules["prefect"] = types.ModuleType("prefect")` when the real package is absent, then imports `pyutilz.system.scheduling.prefect` against it. Neither entry is ever removed: no fixture, no `try/finally`, no `monkeypatch.setitem`. Both the empty stub and the module bound against it persist in `sys.modules` for every test that runs afterwards in the same process. Latent today — I ran `tests/test_system_audit_fixes.py tests/test_meta/test_optional_deps_isolation.py` in one process (55 passed) and the isolation suite is unaffected because it re-executes its scenarios in subprocesses.
- **Failure scenario**: any future in-process test that probes for prefect — an `importlib.util.find_spec("prefect")` capability check, or an optional-dependency assertion moved out of a subprocess — sees a package that is installed but has no attributes, and its result depends on file order. This is the class the repo already bans for `importlib.reload` and (since 2026-09-02 F15) polices for `sys.modules.pop` in `tests/test_meta/test_no_module_reload.py`; assignment into `sys.modules` is not covered by that check.
- **Suggested fix**: install the stub through a fixture that removes both keys on teardown (or `monkeypatch.setitem`), and extend `tests/test_meta/test_no_module_reload.py`'s AST scan to `sys.modules[...] = ...` writes with the reviewed-whitelist mechanism it already uses for `pop`. `tests/test_web_extra.py:454` shows the correct `try/finally` shape.

### F12. [Low] `test_safe_dump_and_disk_cache_create_with_explicit_mode` matches `0o600` anywhere in a whole module's source — tests/test_security_audit_20260902.py:251
- **Disposition**: COMPLETED — both getsource string matches are replaced by behavioural checks in tests/test_security_audit_20260902.py: `safe_dump()` and `DiskCache.put()` now write real files under an `os.open` spy that asserts the CREATING call used an owner-only mode (`& 0o077 == 0`), which -- unlike the `_POSIX_ONLY` result-mode assertions -- also runs on the Windows CI leg; the POSIX result-mode checks are kept and extended with `test_safe_dump_result_is_owner_only`.
- **Category**: weak-assertion
- **Problem**: A sharper instance of F02, separated because the fix differs. Line 251 asserts `"0o600" in inspect.getsource(safe_pickle)` — `safe_pickle` is the **module**, not a function, so the literal may appear in a comment, a docstring, an unused constant, or an unrelated function. Line 252 is the same over `disk_cache.DiskCache.put`. The `_POSIX_ONLY` behavioural counterpart that exists for `atomic_write_bytes` (`tests/test_security_audit_20260902.py:254`, asserting `_mode(target) & 0o077 == 0` on a real file) has no equivalent for either of these two, and being POSIX-gated it does not run on the Windows leg of the CI matrix at all.
- **Failure scenario**: the mode constant surviving only in a comment while the `os.open` call loses its mode argument — or `DiskCache.put` switching to a plain `open()` with `0o600` left behind in a nearby docstring — keeps the test green while cache and pickle files land group- and world-readable, the exact vulnerability the test was written for.
- **Suggested fix**: replace both with `_POSIX_ONLY` behavioural checks that write a real file through `safe_pickle`'s dump entry point and through `DiskCache.put`, then assert `_mode(path) & 0o077 == 0`, mirroring line 254.
