# Documentation Audit — pyutilz (2026-09-02)

## Summary

Read in full: `README.md`, `docs/index.md`, `docs/modules.md`, `docs/guides/{llm_providers,kernel_tuning_cache,safe_pickle}.md`, `mkdocs.yml`, `CHANGELOG.md`, `CONTRIBUTING.md`, `TESTING.md`, `CLAUDE.md`, plus `pyproject.toml` (dependencies/extras/coverage/pytest config), `.pre-commit-config.yaml` and `.github/workflows/*`. Prior reports `audits/2026-07-21_full-audit/05-documentation.md` and `audits/2026-07-21_audit-round2/INDEX.md` were read first; all 18 documentation findings from that round were re-checked against the current tree and every one is confirmed FIXED (the SQL example now reads `"...".format(table), (user_id,)` at README.md:258; `pyutilz.stats` is now README.md:58 + docs/modules.md:41-43; `code_audit` is now README.md:56 + docs/modules.md:35; the `matrix.py` CSC docstring, the `llm/base.py` stale docstring, the CONTRIBUTING coverage-omit-list, the `pre-commit install` mention and the bad test-run command all now match the code). Nothing below is a re-raise. Community-health files (SECURITY.md etc.) are dispositioned won't-fix per CLAUDE.md and are not raised.

Verification actually performed (not read-only):

- Imported and, where runnable offline, executed every README/docs code snippet under `D:/ProgramData/anaconda3/python.exe`: `optimize_dtypes`, `showcase_df_columns`, `benchmark_dataframe_compression`, `humanize`/`strip_ai_patterns`/`introduce_typos`, `SentenceSimilarityIndex`, `PortHealthTracker`, `safe_dump`/`safe_load`, `timeout_wrapper`/`log_duration`, `get_max_affordable_workers_count`, `get_system_info`, `KernelTuningCache.load_or_create`/`hw_fingerprint`/`update`/`lookup`, `get_llm_provider`, `list_openrouter_models`, `validate_sql_identifier`/`safe_execute`. All import and run.
- Existence-checked every symbol named in the three guides (`compute_code_version`, `kernel_tuner`, `discover_tuners`, `retune_all`, `RemoteBackend`, `S3Backend`, `array_location`, `provenance_changed`, `verify_sidecar`, `write_sidecar`, `_PROVIDER_MODULES`/`_ALIASES`, `generate_stream` per provider) — all present and matching the prose.
- Ran an AST cross-check of every public function's Google-style `Args:` block against its real signature across `src/pyutilz` (61 `Args:` blocks): zero ghost params, zero partially-documented signatures, zero doc-vs-signature default-value mismatches. The `docstring_args_incomplete` / `return_annotation_mismatch` code_audit scanners are doing their job; docstring coverage is CI-enforced at `interrogate-fail-under: 100` (`.github/workflows/ci.yml:120`).
- Measured: `pytest --collect-only -q` → 3538 tests collected in 78.8 s; `pytest tests/test_meta -q` → 186.7 s (2 pre-existing failures, unrelated to docs). Verified all 5 `docs/` pages are in `mkdocs.yml` nav (no orphans, no stale entries) and all 12 relative Markdown links in README/docs/TESTING resolve to existing paths. Verified all 4 badge workflow names (`CI`, `Black`, `MyPy`, `docs`) match `.github/workflows/*`. Verified TESTING.md's meta-test table: all 13 named test files exist, the "27-entry alias map" is exactly 27 entries, `--refresh-debt-baseline` and the `pyutilz-meta-tests-static-only` hook id both exist.
- Verified pytest `addopts` contains `--strict-markers` and reproduced the resulting collection ERROR for an unregistered marker in an isolated sandbox.

Findings: 3 High, 6 Medium, 12 Low (21 total). All 21 dispositioned COMPLETED on 2026-09-02; no finding was rejected, though F04's stated failure mechanism was corrected (see its disposition) and F08/F17 were fixed by deleting the drifting number/list rather than refreshing it.

Two items deferred here by the packaging audit (`audits/2026-09-02/03-packaging-dependencies.md`) were folded into this pass:

- **Extras / "no hard deps" documentation** (deferred from 03's F02 and F09). `pyproject.toml` is now the source of truth and the docs were rewritten against it, not against the pre-2026-08 shape: 9 core dependencies, 16 extras groups all listed, `[all]`'s four deliberate exclusions (gpu/dash/prefect/tensorflow) stated with their recorded rationale, `[dev]` no longer carrying py-ci-shared (which installs via the new `requirements-dev.txt`), `[stats]` annotated as the documented empty alias it became, and the `python_version` markers on `[nlp]`'s spacy and `[llm]`'s google-genai surfaced. Covered by F01, F03, F05, F12, F13, F14 and F15. CLAUDE.md's one stale line saying py-ci-shared installs via `pip install -e ".[dev]"` was corrected in the same pass (CLAUDE.md:11) - the only file outside this agent's ownership that was touched, and only for that line.
- **CHANGELOG `[Unreleased]`** (deferred from 03's F02 and F15, on top of this file's own F06). Beyond F06's five missing entries it gained a packaging-metadata block (CHANGELOG.md:31-39, including the `pypiwin32` drop from `[system]` that 03/F15 asked for and the contributor note that `pre-commit`/`vulture` are still undeclared) and a "Third full-repo audit wave" entry (CHANGELOG.md:57) written from `git show 2e3e710`/`4479076` and the dispositions in `audits/2026-09-02/*.md`: 223 findings across ten directions, the 133 from the packaging and three domain directions landed with 300+ regression tests, the architecture/security/typing/test-quality/documentation/performance directions in the same wave.

## Findings

### F01. [High] `pip install pyutilz` documented as having "no hard deps" — it installs 9 packages including pandas — docs/index.md:23
- **Disposition**: COMPLETED - docs/index.md:23 and README.md:21 now list the real nine-package core, and the prose at README.md:14 / docs/index.md:14 was rewritten to name all nine (numba, numpy, joblib, portalocker, psutil, pandas, tqdm, pympler, plus tomli below 3.11) with the reason they are core, keeping the "domain extras are opt-in" point. Re-verified against the post-audit-wave pyproject.toml:52-105, which is unchanged in this respect.
- **Category**: docs/correctness
- **Problem**: `docs/index.md:23` (the published documentation site's front page) says `pip install pyutilz  # core only, no hard deps`. `pyproject.toml:27-97` declares `[project.dependencies]` = `numba>=0.56`, `numpy>=1.20`, `joblib>=1.2`, `portalocker>=2.0`, `psutil>=5.9`, `pandas>=1.5`, `tqdm>=4.65`, `pympler>=1.0`, and `tomli>=2.0 ; python_version < '3.11'` — nine unconditional runtime dependencies, four of them (`numba`, `numpy`, `pandas`, `psutil`) heavyweight. README.md:21 states the same install line as `# core only (numba, joblib, portalocker)`, which is also wrong but only by omission; README.md:14 and docs/index.md:14 repeat the three-package framing in prose ("The core has few hard dependencies (`numba`, `joblib`, `portalocker` ...)"), followed by "so `pip install pyutilz` stays light".
- **Failure scenario**: A reader evaluating pyutilz for a size- or dependency-constrained context (a Lambda layer, a container image, a library that must not drag pandas into its consumers' resolver) reads "no hard deps", adds pyutilz, and only discovers at `pip install` time that they have pulled the entire numba+numpy+pandas stack. The claim is also the library's stated selling point in `pyproject.toml:8`'s package description, so it is load-bearing for the adoption decision.
- **Suggested fix**: Replace docs/index.md:23's comment with the real list (`numba, numpy, joblib, portalocker, psutil, pandas, tqdm, pympler`), sync README.md:21, and reword the README.md:14 / docs/index.md:14 prose from "few hard dependencies (numba, joblib, portalocker)" to the accurate set, keeping the "domain extras are opt-in" point, which remains true.

### F02. [High] `pre-commit install` is instructed in three docs but `pre-commit` is in no extras group — README.md:38
- **Disposition**: COMPLETED - neither `pre-commit` nor `vulture` is declared in any extras group or in the new requirements-dev.txt (re-verified against the current pyproject.toml), so all four setup docs now install them explicitly before `pre-commit install`: README.md:58, docs/index.md:60, CONTRIBUTING.md:70-74 (with the failure mode spelled out) and its Optional-Dependencies list at CONTRIBUTING.md:94, plus TESTING.md:99. The same blocks also gained `pip install -r requirements-dev.txt`, since py-ci-shared left the [dev] extra in the packaging pass.
- **Category**: docs/correctness
- **Problem**: README.md:36-39, docs/index.md:38-41 and CONTRIBUTING.md:64-67 all give the same setup sequence: `pip install -e ".[all,dev]"` immediately followed by `pre-commit install`. `grep -n "pre-commit" pyproject.toml` returns only comment lines (319, 323, 325) — `pre-commit` is not a dependency of `[dev]` (`pyproject.toml:300-327`, whose full package list is numpy, pytest, pytest-cov, pytest-benchmark, pytest-asyncio, pytest-instafail, pytest-progress, pytest-timeout, black, ruff, mypy, bandit, py-ci-shared), nor of any other group, nor of `[all]` (`pyproject.toml:295-297`). The same applies to `vulture`, which `.pre-commit-config.yaml:181-183` runs as a BLOCKING local hook via `python -m vulture` — `import vulture` fails in this environment (`ModuleNotFoundError: No module named 'vulture'`) after the documented dev install.
- **Failure scenario**: A new contributor follows the documented three-line setup verbatim; `pre-commit install` fails with "command not found" on the very first onboarding step. If they install `pre-commit` themselves, their first commit then fails inside the blocking `vulture-blocking` hook with `No module named vulture`, which reads as a broken repo rather than a missing undeclared tool.
- **Suggested fix**: Add `pre-commit>=3.0` and `vulture>=2.7` to the `[dev]` extras group in `pyproject.toml`; or, if they are deliberately excluded, add an explicit `pip install pre-commit vulture` line before `pre-commit install` in all three docs.

### F03. [High] `pyutilz.core` described as having "zero hard dependencies" — its modules import numpy, scipy, Pillow, numba, joblib, portalocker — docs/modules.md:7
- **Disposition**: COMPLETED - docs/modules.md:7 no longer claims "zero hard dependencies"; it now says the core subpackage is the lightest but not dependency-free, and names which module needs what (matrix.py -> scipy, image.py -> Pillow, both in the `[system]` extra; pythonlib.py -> numba/joblib/portalocker, which are core). `disk_cache` was added to the same sentence while there.
- **Category**: docs/correctness
- **Problem**: docs/modules.md:7 opens the `pyutilz.core` section with "Core Python helpers with zero hard dependencies". Verified module-level imports inside `src/pyutilz/core/`: `matrix.py:20-21` imports `numpy` and `from scipy.sparse import csr_matrix, csc_matrix, coo_matrix`; `image.py:25-28` imports `PIL.Image`, `PIL.ExifTags`, `PIL.TiffImagePlugin`, `PIL.ImageFile`; `pythonlib.py:31,37,878` imports `numba`, `joblib`, `portalocker`. `pyproject.toml:262-265` documents this in the opposite direction ("core/image.py and core/matrix.py import Pillow/scipy unconditionally at module level"), which is exactly why Pillow and scipy are pinned into the `[system]` extra.
- **Failure scenario**: A reader who wants only `pyutilz.core.matrix` installs bare pyutilz (which includes neither scipy nor Pillow — see `pyproject.toml:27-97`) and gets `ModuleNotFoundError: No module named 'scipy'` on import, having been told the subpackage has zero dependencies. No extras group is named for `core`, so the doc offers no recovery path either.
- **Suggested fix**: Replace "zero hard dependencies" with an accurate statement, e.g. "the lightest subpackage — `matrix.py` needs scipy and `image.py` needs Pillow, both shipped in the `[system]` extra; the rest needs only the package's own core deps".

### F04. [Medium] CONTRIBUTING tells contributors to use `@pytest.mark.integration`, which is a collection ERROR under this repo's `--strict-markers` — CONTRIBUTING.md:221
- **Disposition**: COMPLETED - CONTRIBUTING.md:241-266 rewritten: the `integration` example is gone (replaced by `live`, which is registered and which TESTING.md depends on), the three registered markers are named against pyproject.toml:390-394, and `pytest -m integration` was dropped from the run-categories block in favour of `-m gpu` and `--run-live -m live`. Verified both replacement commands actually select (`pytest -m gpu --collect-only` -> 2 selected; `pytest --run-live -m live --collect-only` -> 10 selected). The audit's stated mechanism was corrected rather than repeated: in an isolated sandbox on the installed pytest 9.0.1, `--strict-markers` errors only when passed on the COMMAND LINE - coming from `addopts` (as it does here) it currently yields just a PytestUnknownMarkWarning - so the doc states the contract and flags the version-dependence instead of promising a collection error.
- **Category**: docs/correctness
- **Problem**: CONTRIBUTING.md:214-235 ("Test Categories") instructs `@pytest.mark.slow`, `@pytest.mark.integration`, `@pytest.mark.gpu`, then `pytest -m integration  # Only integration tests`. `pyproject.toml`'s `[tool.pytest.ini_options] markers` list registers exactly three markers — `slow`, `gpu`, `live` — and NOT `integration`. `pyproject.toml:355` sets `addopts = "-v --tb=short --strict-markers -ra --timeout=120"`. Reproduced in an isolated sandbox: with `--strict-markers`, an unregistered `@pytest.mark.integration` produces `ERROR collecting test_p.py — 'integration' not found in \`markers\` configuration option` followed by `Interrupted: 1 error during collection` (without the flag it is only a `PytestUnknownMarkWarning`). CONTRIBUTING also never mentions the `live` marker that does exist and that TESTING.md:4 depends on.
- **Failure scenario**: A contributor follows the contributing guide, marks a new test `@pytest.mark.integration`, and the entire test session aborts at collection — not just their test. The error text points at their test file, not at the doc that told them to write it, so they lose time before finding the real cause.
- **Suggested fix**: Replace `integration` with `live` in the CONTRIBUTING example (or register an `integration` marker in `pyproject.toml`), and add a sentence noting that `--strict-markers` is on, so any new marker must be registered first.

### F05. [Medium] `[all,dev]` is called the "full install (recommended)" but omits four extras groups, including the one `pyutilz.dev.dashlib` needs — README.md:19
- **Disposition**: COMPLETED - README.md:38-49 and docs/index.md:40-51 now spell out `[all]`'s membership, name the four deliberate exclusions (gpu/dash/prefect/tensorflow) with the rationale from pyproject.toml's `all` and `gpu` comments, and state the consequence (`pyutilz[all]` cannot import pyutilz.dev.dashlib or pyutilz.system.scheduling.prefect). `[dash]`, `[prefect]`, `[tensorflow]` and `[gpu]` also gained install lines of their own (README.md:30-33, docs/index.md:32-35), and CONTRIBUTING.md:97-99 repeats the caveat for contributors.
- **Category**: docs/completeness
- **Problem**: README.md:19 and docs/index.md:21 present `pip install pyutilz[all,dev]` as the "full install (recommended)". `pyproject.toml:295-297` defines `all = ["pyutilz[pandas,polars,database,web,cloud,nlp,llm,system,stats]"]` — it deliberately excludes `gpu` (documented at `pyproject.toml:286-291`) and silently excludes `dash` (`pyproject.toml:187-193`: flask, dash, dash-bootstrap-components), `prefect` (`pyproject.toml:196-198`) and `tensorflow` (`pyproject.toml:201-203`). `dash` is not an obscure corner: README.md:56 advertises "dashboards" in the `pyutilz.dev` row, docs/modules.md:35 repeats it, and CHANGELOG.md:11 makes `dashlib.create_tabs`'s new `show_loading` argument the single entry in "[Unreleased] > Changed". `pyproject.toml:187-190` states plainly that `dev/dashlib.py` imports flask/dash/dash_bootstrap_components unconditionally at module level.
- **Failure scenario**: A reader performs the "full install (recommended)", follows the docs to the advertised dashboard helpers, and gets `ModuleNotFoundError: No module named 'dash'` — with no doc anywhere naming the `[dash]` extra that fixes it (`grep -rn "\[dash\]" README.md docs/ CONTRIBUTING.md TESTING.md` → no matches).
- **Suggested fix**: Either add `dash`/`prefect`/`tensorflow` to `all`, or state at README.md:19 / docs/index.md:21 what `[all]` intentionally leaves out and list the `[dash]`, `[prefect]`, `[tensorflow]`, `[gpu]` extras in the install block below it.

### F06. [Medium] CHANGELOG "[Unreleased]" is stale by 27 landed commits, including two new public constants and three behavior fixes — CHANGELOG.md:8
- **Disposition**: COMPLETED - CHANGELOG.md [Unreleased] brought up to date: Added gained ALLOWED_LOCATIONS and ALLOWED_COUNTRIES (CHANGELOG.md:15-16), Fixed gained the humanizer repeated-protected-span fix, the 15%->30% LLM context reserve and the polars_df_info RangeIndex removal (CHANGELOG.md:49-51), and Changed gained the codecov-full workflow/badge chain (CHANGELOG.md:41). Each entry was written from the commit body rather than the subject line. The same pass also added the deferred packaging entry (CHANGELOG.md:31-39) and the audit-wave entry (CHANGELOG.md:57) - see the Summary note below.
- **Category**: docs/process
- **Problem**: `git log -1 --format=%cd -- CHANGELOG.md` → `Tue Aug 25 16:19:14 2026`. `git log --oneline --since=<that>` → 27 commits, none reflected in the Unreleased section. Verified absent by keyword grep against CHANGELOG.md (0 hits each): `ea71213` "decodo: add ALLOWED_COUNTRIES for the datacenter endpoint" and `6dbd5a7` "decodo: add ALLOWED_LOCATIONS, the account's full exit inventory" — both add new module-level public constants in `src/pyutilz/web/proxy/decodo.py`; `3f24162` "fix: polars_df_info claimed a RangeIndex that polars frames do not have" — a user-visible output-shape fix; `02ffe5b` "llm: size the context reserve from the worst measured tokeniser undercount" — changes the effective output budget of every LLM call, i.e. exactly the class of change CHANGELOG.md:38-40's existing `fit_max_tokens_to_context` entry was written for; `5cc907c` "humanizer: protect every occurrence of a repeated protected span" — a correctness fix in a README-advertised API (README.md:159-168); and `b59b0ed`/`c04ab03`/`63e283d`/`17cfe7b`, the new `codecov-full` workflow whose badge README.md:8 already renders. CONTRIBUTING.md:303 and CONTRIBUTING.md:315 make "Update CHANGELOG.md under Unreleased" a mandatory PR step, so the project's own process treats this as required.
- **Failure scenario**: A downstream consumer (mlframe) reads CHANGELOG.md before upgrading, sees no LLM entry, and does not learn that the effective `max_tokens` for every request changed — a silent behavioural difference they will instead debug from the symptom. Likewise the new `ALLOWED_COUNTRIES`/`ALLOWED_LOCATIONS` constants are undiscoverable outside the source.
- **Suggested fix**: Add `### Added` entries for the two decodo constants and `### Fixed` entries for `polars_df_info`, the humanizer protected-span fix, and the LLM context-reserve sizing, before the next release cut.

### F07. [Medium] README's parallel-apply example passes the deprecated `n_cores=` parameter — README.md:186
- **Disposition**: COMPLETED - README.md:207 now passes `n_jobs=n`. Verified by running the corrected example under `warnings.simplefilter('error', DeprecationWarning)`: it completes with no warning, where the `n_cores=` spelling raises.
- **Category**: docs/correctness
- **Problem**: README.md:186 shows `results = applyfunc_parallel(iterable=inputs, func=expensive_fn, n_cores=n, return_dataframe=False)`. `src/pyutilz/system/parallel.py`'s `applyfunc_parallel` docstring states "``n_cores`` is a deprecated alias for ``n_jobs`` (kept for backward compatibility)", and the function's first statement is `if n_cores is not None: warnings.warn("applyfunc_parallel's \`n_cores\` parameter is deprecated; use \`n_jobs\` instead.", DeprecationWarning, stacklevel=2)`. The current signature is `applyfunc_parallel(iterable, func, n_jobs=None, return_dataframe=True, logical=False, initializer=None, initargs=(), use_threads=False, n_cores=None)`. `tests/test_meta/test_deprecation_warnings_present.py` exists to enforce that this warning fires, so the deprecation is deliberate — yet the README teaches the deprecated spelling.
- **Failure scenario**: Every reader who copies the README's flagship parallelism example writes code that emits a `DeprecationWarning` on first call, and projects running `-W error::DeprecationWarning` in CI fail outright on code copied from the library's own front page.
- **Suggested fix**: Change README.md:186 to `n_jobs=n`.

### F08. [Medium] "1900+ tests" is understated by ~1600 tests; the paired 79.6% coverage figure is undated and uncorroborated — README.md:274
- **Disposition**: COMPLETED - the hard numbers are gone rather than refreshed, applying the convention CHANGELOG.md's meta-test section already set. README.md:295-299 and TESTING.md:3-6 now point at the three codecov badges for coverage and at `pytest --collect-only -q` for the count. (Measured while deciding: 3774 tests at the start of this pass, 3851 an hour later as a concurrent agent added tests - which is exactly why no count was pinned. The 79.6% figure remains uncorroborated; the stale htmlcov/ artifact is not evidence either way.)
- **Category**: docs/staleness
- **Problem**: README.md:274 and TESTING.md:3 both read "1900+ tests, 79.6% line coverage on `src/pyutilz/`". Measured: `python -m pytest --collect-only -q --no-cov` → `3538 tests collected in 78.82s`. The coverage figure carries no date or commit reference, and the only coverage artifact in the tree (`htmlcov/index.html`, dated Feb 18 2026) reports `22.98%`, i.e. it is a stale artifact from a partial run and cannot corroborate the 79.6% claim in either direction. CHANGELOG.md's meta-test section already adopted the right policy for this class of number ("Exact test/file counts are intentionally not pinned here — they drift with every meta-test addition; run `pytest tests/test_meta --collect-only -q` for the current count"); README and TESTING did not follow it.
- **Failure scenario**: A reader assessing test maturity gets a count that is 45% low and has no way to tell whether the coverage percentage beside it is equally stale — which devalues both numbers rather than just the wrong one.
- **Suggested fix**: Update to "3500+ tests", or apply the CHANGELOG's own convention and drop the hard counts in favour of the Codecov badge (already rendered at README.md:6-8) plus a `--collect-only` pointer.

### F09. [Medium] `pyutilz.data.git_checkpoint_cache` — a CHANGELOG headline addition — is absent from both module-orientation docs — README.md:49
- **Disposition**: COMPLETED - `git_checkpoint_cache` added to README.md:70's `pyutilz.data` row and given two sentences naming all four public functions at docs/modules.md:11.
- **Category**: docs/completeness
- **Problem**: `src/pyutilz/data/git_checkpoint_cache.py` ships four public functions (`write_json_checkpoint`, `read_json_with_checkpoint_fallback`, `write_bytes_checkpoint`, `read_bytes_with_checkpoint_fallback` — verified importable) and is the second entry under CHANGELOG.md:14's "[Unreleased] > Added", with a full paragraph of rationale. README.md:49's `pyutilz.data` row lists only "`pandaslib`, `polarslib`, `numpylib`, `numbalib`", and docs/modules.md:11's `pyutilz.data` paragraph names only `optimize_dtypes`, `benchmark_dataframe_compression`, `showcase_df_columns`. `grep -rn "git_checkpoint" README.md docs/` → zero matches. This is the identical pattern to the 2026-07-21 `code_audit` finding (since fixed), recurring on the next new module.
- **Failure scenario**: A reader who sees the CHANGELOG entry and turns to the orientation docs — the documented next step for "what does this subpackage contain" — finds no trace of it; a reader who never opens CHANGELOG never learns the module exists.
- **Suggested fix**: Add `git_checkpoint_cache` to README.md:49's list and one sentence to docs/modules.md:11's paragraph.

### F10. [Low] CONTRIBUTING points at `tests/benchmark_*.py`; benchmarks actually live in `_benchmarks/bench_*.py` — CONTRIBUTING.md:357
- **Disposition**: COMPLETED - CONTRIBUTING.md:393-397 now points at `_benchmarks/bench_*.py` with the `python -m _benchmarks.bench_*` invocation, and explicitly warns against confusing them with tests/test_kernel_tuning_benchmark.py and tests/test_dev_benchmarking.py, cross-linking TESTING.md's "Test layout" paragraph.
- **Category**: docs/correctness
- **Problem**: CONTRIBUTING.md:357 says "**Benchmark**: Use `tests/benchmark_*.py` scripts to measure performance". No file matches that glob: `tests/` contains `test_dev_benchmarking.py` and `test_kernel_tuning_benchmark.py`, both pytest suites rather than scripts. The real standalone benchmark scripts are `_benchmarks/bench_classify_column_types.py`, `bench_get_columns_of_type.py`, `bench_morpheme_tokenize.py`, `bench_pandaslib.py`, `bench_showcase_df_columns.py`. TESTING.md:85-91 documents this correctly, including the `python -m _benchmarks.bench_*` invocation and an explicit warning that these four surfaces are "easy to confuse by name" — CONTRIBUTING is the one that got it wrong.
- **Failure scenario**: A contributor asked by CONTRIBUTING.md:360 to include benchmark results in their PR looks for `tests/benchmark_*.py`, finds nothing, and either writes an ad-hoc script or skips the step.
- **Suggested fix**: Change CONTRIBUTING.md:357 to reference `_benchmarks/bench_*.py`, cross-linking TESTING.md's "four benchmark surfaces" paragraph.

### F11. [Low] Documented runtimes are ~6x optimistic for the meta-test suite (measured 187 s vs "~30 s") — TESTING.md:11
- **Disposition**: COMPLETED - re-measured on this box and both figures corrected: `pytest tests/test_meta -q --no-cov -p no:randomly` = 114.99 s (TESTING.md:16 and TESTING.md:102 now say ~2 min, not ~30 s / ~30-60 s), and the manual static-only variant, measured with the hook's own deselect list, = 63.54 s (TESTING.md:106 now says ~1 min, not ~5 s). TESTING.md:8-9 adds that these are one Windows dev box on 2026-09-02 and scale with hardware, and TESTING.md:108-110 moves the fast-path advice up beside the runtime with a note that `--no-verify` skips every other gate too. The unverified full-suite "~3 min" was dropped rather than guessed at (TESTING.md:15, README.md:300).
- **Category**: docs/staleness
- **Problem**: TESTING.md:11 says `pytest tests/test_meta/  # static meta-tests only, ~30 s`, and TESTING.md:97 says the pre-commit hook "runs the meta-test suite on every commit (~30-60 s)". Measured on this box: `python -m pytest tests/test_meta -q --no-cov` → `2 failed, 101 passed, 3 warnings in 186.71s (0:03:06)`. Related: README.md:279 and TESTING.md:10 say the full suite takes "~3 min", while bare collection of the full 3538-test suite alone took 78.8 s, so that figure is likely stale by a similar factor (not directly measured here — the full run was not executed). Timings are hardware-dependent, but the gap is large enough to change behaviour.
- **Failure scenario**: A contributor sizing whether to run meta-tests in their inner loop budgets 30 s, gets 3 minutes, and starts using `--no-verify` to skip the blocking pre-commit hook — the exact outcome the "~5 s manual-stage variant" note at TESTING.md:99-104 exists to prevent.
- **Suggested fix**: Re-measure and update both figures (or qualify them with reference hardware), and move the `--hook-stage=manual` fast-path mention up next to the runtime claim.

### F12. [Low] `[system]` extras description lists three packages that are now core and omits the group's two heaviest members — README.md:28
- **Disposition**: COMPLETED - README.md:28 / docs/index.md:30 now read `scipy + Pillow + py-cpuinfo + GPUtil + xmltodict + jellyfish`, dropping psutil/numba/tqdm (core since the packaging pass) and surfacing the group's two heaviest members. Cross-checked member-by-member against pyproject.toml's `system` group, which also no longer redeclares pandas/tqdm/pympler.
- **Category**: docs/staleness
- **Problem**: README.md:28 and docs/index.md:30 read `pip install pyutilz[system]  # psutil + numba + GPUtil + tqdm + py-cpuinfo`. Per `pyproject.toml:250-283`, `psutil`, `numba` and `tqdm` were promoted to `[project.dependencies]` (core) and are no longer this group's distinguishing content; the group actually contains `tqdm`, `pympler`, `pandas`, `python-dateutil`, `Pillow`, `scipy`, `py-cpuinfo`, `GPUtil`, `xmltodict`, `jellyfish` — with scipy and Pillow, the two heaviest members, unmentioned.
- **Failure scenario**: A reader budgeting install size for `[system]` sees five smallish packages and does not anticipate scipy and Pillow; conversely a reader who wants numba assumes they must take `[system]` to get it.
- **Suggested fix**: Update the comment to the group's real headline members, e.g. `scipy + Pillow + py-cpuinfo + GPUtil + pympler + xmltodict`.

### F13. [Low] `[llm]` extras description omits `pydantic-settings` and `tiktoken` — README.md:27
- **Disposition**: COMPLETED - README.md:27 / docs/index.md:29 now append `+ pydantic-settings + tiktoken`, and also carry the `google-genai` python_version>='3.9' marker that landed in the packaging pass.
- **Category**: docs/completeness
- **Problem**: README.md:27 / docs/index.md:29 read `pip install pyutilz[llm]  # anthropic + google-genai + httpx + tenacity + pydantic`. `pyproject.toml:239-247` also includes `pydantic-settings>=2.0` and `tiktoken>=0.5`. `pydantic-settings` is not incidental: README.md:269-271 and docs/index.md:98-100 both state that "LLM API keys are read from `.env` via `pydantic-settings`", so the Security section depends on a package the install line does not list.
- **Failure scenario**: A reader auditing the LLM extra's dependency footprint, or hand-installing the same set into another environment, misses two packages, one of which is required for the documented `.env` key-loading path.
- **Suggested fix**: Append `+ pydantic-settings + tiktoken` to both lines.

### F14. [Low] `[dev]` extras description omits six pytest plugins and `py-ci-shared`, and does not warn that `black` is skipped below Python 3.10 — README.md:29
- **Disposition**: COMPLETED - README.md:35 / docs/index.md:37 now name the pytest plugin set and mark black as >=3.10 only. `py-ci-shared` was deliberately NOT added to that line: the packaging pass removed it from `[dev]` entirely (a git+https direct reference makes the distribution unpublishable), so the accurate fix is the separate `pip install -r requirements-dev.txt` line now present in every setup block.
- **Category**: docs/completeness
- **Problem**: README.md:29 / docs/index.md:31 read `pip install pyutilz[dev]  # pytest, ruff, black, mypy, bandit`. `pyproject.toml:300-327` additionally installs `numpy`, `pytest-cov`, `pytest-benchmark`, `pytest-asyncio`, `pytest-instafail`, `pytest-progress`, `pytest-timeout`, and `py-ci-shared` (a git dependency). Two entries carry environment markers — `black==26.5.1 ; python_version >= '3.10'` and `py-ci-shared ... ; python_version >= '3.9'` — so on 3.8/3.9 the install silently omits `black`, which README lists unconditionally. CONTRIBUTING.md:49-54 documents this marker behaviour correctly; README and docs/index.md do not.
- **Failure scenario**: A contributor on Python 3.9 installs `[dev]`, is told black is included, and gets "no module named black" when they try to format — a failure CONTRIBUTING explains but the two docs they read first do not.
- **Suggested fix**: Add "(black requires Python >=3.10)" to the README/index line and mention that the group also pulls the pytest plugin set.

### F15. [Low] The install block omits 8 of the 16 declared extras groups, including `[stats]` and `[docs]` — README.md:19
- **Disposition**: COMPLETED - the install block now covers all 16 groups: `stats`, `dash`, `prefect`, `tensorflow`, `gpu` and `docs` added at README.md:29-34 / docs/index.md:31-36, with `[stats]` annotated as the documented empty alias it became in the packaging pass. CONTRIBUTING.md gained a "Building the documentation" section (CONTRIBUTING.md:101-110) with `pip install -e ".[docs]"` + `mkdocs serve` and a note that mkdocs.yml's `strict: true` makes a broken internal link fail the build; docs/index.md:65 carries the one-line version.
- **Category**: docs/completeness
- **Problem**: README.md:19-30 and docs/index.md:21-32 list 9 install lines covering `all`, `dataframes`, `database`, `web`, `cloud`, `nlp`, `llm`, `system`, `dev`. `pyproject.toml` declares 16 groups; additionally present are `pandas`, `polars`, `dash`, `prefect`, `tensorflow`, `stats`, `gpu`, `docs`. `[stats]` (`pyproject.toml:276-281`) is the only targeted way to get the `pyutilz.stats` subpackage that README.md:58 advertises as its own module row; `[docs]` (`mkdocs-material`, `pyproject.toml:329-332`) is the only way to build the site `mkdocs.yml` configures, and neither CONTRIBUTING.md nor TESTING.md mentions it (`grep -rn "mkdocs" README.md CONTRIBUTING.md TESTING.md` → no matches).
- **Failure scenario**: A contributor asked to "update documentation" (CONTRIBUTING.md:301) has no documented way to build or preview the MkDocs site locally; a user wanting only `pyutilz.stats` installs all of `[all]`.
- **Suggested fix**: Add `stats`, `gpu`, `dash`, `docs` lines to the install block and a "Building the docs" snippet (`pip install -e ".[docs]" && mkdocs serve`) to CONTRIBUTING.md.

### F16. [Low] Twelve shipped modules appear in no documentation at all — README.md:48
- **Disposition**: COMPLETED - all twelve modules are now documented. README.md:69-77 extended the core/data/web/system/text/dev rows (disk_cache, git_checkpoint_cache, url_guard, cached_client, Decodo, gpu_dispatch, resilience, single_flight_cache, cli_logging, config, secrets_scrub, ci_log_analyzer, freevar_analysis) and docs/modules.md:7,11,19,27,31,35 gained a clause each. Every description was taken from the module's own docstring, not inferred from the filename (which is what caught "size-bounded" vs the actual content-addressable disk_cache).
- **Category**: docs/completeness
- **Problem**: `grep -rn "<name>" README.md docs/ CHANGELOG.md` returns zero matches for each of: `core/disk_cache.py`, `web/url_guard.py`, `web/cached_client.py`, `web/proxy/decodo.py`, `system/gpu_dispatch.py`, `system/resilience.py`, `system/single_flight_cache.py`, `system/cli_logging.py`, `system/config.py`, `text/secrets_scrub.py`, `dev/ci_log_analyzer.py`, `dev/freevar_analysis.py`. Several are substantial public surface: `gpu_dispatch` is a GPU backend selector directly adjacent to the heavily documented `performance.kernel_tuning`; `secrets_scrub` and `url_guard` are security-relevant helpers in a package whose README carries a Security section. docs/modules.md:3 explicitly frames these pages as orientation rather than exhaustive listing, which is why this is Low — but the omissions cluster in exactly the areas the README already claims to cover.  <!-- pragma: allowlist secret -->
- **Failure scenario**: A user needing SSRF-style URL validation or secret scrubbing re-implements it, or adds a third-party dependency, because pyutilz's own docs give no signal these exist.
- **Suggested fix**: Extend the `pyutilz.web`, `pyutilz.system`, `pyutilz.text`, `pyutilz.core` and `pyutilz.dev` rows in README.md:48-58 (and their docs/modules.md paragraphs) by a clause each naming the strongest of these.

### F17. [Low] CHANGELOG's "Module Categories" reference section is stale by ~19 modules — CHANGELOG.md:82
- **Disposition**: COMPLETED - the "Module Categories" section was deleted rather than refreshed (CHANGELOG.md:95-101 now carries a short tombstone explaining why and pointing at docs/modules.md and README.md's Modules table, which are the maintained equivalents). Refreshing it would have re-created a second hand-maintained package map that drifts the same way.
- **Category**: docs/staleness
- **Problem**: CHANGELOG.md's trailing "Module Categories" section lists 31 module names across seven categories. Cross-checked against `src/pyutilz/`, it never gained: `safe_pickle`, `disk_cache`, `git_checkpoint_cache`, `humanizer`, `secrets_scrub`, `normality` (the entire `stats` subpackage), `kernel_tuning` (the entire `performance` subpackage), `gpu_dispatch`, `resilience`, `single_flight_cache`, `cli_logging`, `config`, `url_guard`, `cached_client`, `psycopg2_pool`, `meta_test_utils`, `notebook_init`, `ci_log_analyzer`, `freevar_analysis`. The 2026-07-21 round fixed this section's one *wrong* entry (`com`, now annotated as retired at v1.0.0) but not its incompleteness; it now describes roughly the v1.0.0 tree.
- **Failure scenario**: A reader treats the categorised list as the package map and concludes there is no kernel-tuning or safe-pickle module — both of which have their own dedicated guides.
- **Suggested fix**: Either refresh the list or delete the section and point at `docs/modules.md`, which is the maintained equivalent.

### F18. [Low] safe_pickle guide's "Fail-closed by default" paragraph attributes a raise to `verify_sidecar`, which only ever returns a bool — docs/guides/safe_pickle.md:30
- **Disposition**: COMPLETED - docs/guides/safe_pickle.md:30 now says a digest mismatch makes `verify_sidecar()` return `False` regardless of the env var (the opt-in covers a MISSING sidecar only) and that `safe_load()` is what turns that into `PickleVerificationError`, with an explicit note that `verify_sidecar()` never raises so a direct caller must check the return value. Verified end-to-end: a corrupted payload gives `verify_sidecar() -> False` (also with PYUTILZ_ALLOW_UNVERIFIED_PICKLE=1) while `safe_load()` raises PickleVerificationError.
- **Category**: docs/correctness
- **Problem**: docs/guides/safe_pickle.md:30 reads: "`verify_sidecar()` returns `False` (refuses the load) when the sidecar is missing, unless the environment variable `PYUTILZ_ALLOW_UNVERIFIED_PICKLE` is set truthy ... A digest **mismatch** always raises `PickleVerificationError`, regardless of the env var". `verify_sidecar` (`src/pyutilz/core/safe_pickle.py`) is annotated `-> bool`, and its own docstring contract is explicit — "sidecar present + digest does NOT match -> False"; the mismatch path contains no `raise`, it simply returns `hmac.compare_digest(expected, actual)`. The `PickleVerificationError` is raised one level up, by `safe_load`. The guide's own API-surface list six lines later (docs/guides/safe_pickle.md:36) states the correct behaviour ("`False` on missing/mismatch"), so the page contradicts itself.
- **Failure scenario**: A caller using `verify_sidecar` directly — it is documented as public surface both at line 36 and in the module docstring — writes `try: verify_sidecar(p) except PickleVerificationError: ...` and silently treats every corrupted file as verified, because the falsy return is never checked.
- **Suggested fix**: Reword line 30 to "a digest mismatch makes `verify_sidecar()` return `False` regardless of the env var, and `safe_load()` turns that into a `PickleVerificationError`".

### F19. [Low] CONTRIBUTING's example test imports through the backward-compat alias path, not the canonical one — CONTRIBUTING.md:194
- **Disposition**: COMPLETED - CONTRIBUTING.md:223 now imports `from pyutilz.data.pandaslib import optimize_dtypes`, matching README/docs/modules. Verified the canonical path resolves to the same function object as the alias.
- **Category**: docs/consistency
- **Problem**: CONTRIBUTING.md:194 writes `from pyutilz.pandaslib import optimize_dtypes` in the model test every contributor is shown. The canonical path used everywhere else (README.md:64, README.md:75, README.md:85, docs/index.md:52, docs/modules.md:11) is `from pyutilz.data.pandaslib import ...`. `pyutilz.pandaslib` resolves only via `_MODULE_ALIASES` in `src/pyutilz/__init__.py:13`, whose own comment reads "Module aliases for backward compatibility" (27 entries, verified). The import works today and emits no warning (verified by running it), so this is a consistency issue, not a breakage.
- **Failure scenario**: New tests written from the template accrete on the deprecated import path, making any future removal of the alias map a larger change than it needs to be — and the contributor learns the wrong module layout on their first read.
- **Suggested fix**: Change CONTRIBUTING.md:194 to `from pyutilz.data.pandaslib import optimize_dtypes`.

### F20. [Low] CONTRIBUTING's coverage command measures the repo root, contradicting the omit-list caveat 11 lines above it — CONTRIBUTING.md:171
- **Disposition**: COMPLETED - CONTRIBUTING.md:200 now reads `pytest --cov=src/pyutilz --cov-report=html --cov-report=term-missing`, matching TESTING.md:19 and pyproject.toml's `[tool.coverage.run] source`, so the number the omit-list caveat 11 lines below describes is the number the command produces.
- **Category**: docs/consistency
- **Problem**: CONTRIBUTING.md:171 gives `pytest --cov=. --cov-report=html --cov-report=term-missing`. `--cov=.` overrides `pyproject.toml`'s `[tool.coverage.run] source = ["src/pyutilz"]`, so the measured denominator becomes the whole repo (`tests/`, `scripts/`, `_benchmarks/` included) rather than the package. TESTING.md:14 gives the correct form, `pytest --cov=src/pyutilz --cov-report=term-missing`. This undermines CONTRIBUTING.md:182-187's careful explanation of what the omit list does and does not measure, since a `--cov=.` run does not produce the number that caveat describes.
- **Failure scenario**: A contributor checking whether they met the ">80% coverage for new code" bar (CONTRIBUTING.md:182, CONTRIBUTING.md:302) measures against a different denominator than CI does and draws the wrong conclusion in either direction.
- **Suggested fix**: Change CONTRIBUTING.md:171 to `--cov=src/pyutilz`, matching TESTING.md and CI.

### F21. [Low] The LLM guide's per-provider capability list predates strict JSON-schema output — docs/guides/llm_providers.md:26
- **Disposition**: COMPLETED - docs/guides/llm_providers.md:26 rewritten to the same per-provider precision as its neighbours: `json_schema=` on all three entry points, `supports_json_schema()` (False on the base class -> Anthropic/Gemini/Claude Code; True for the OpenAI-compatible endpoints; a per-model `structured_outputs` catalogue check on OpenRouter that returns False on an unknown model or catalogue failure), the documented degrade-with-warning path, and `last_json_schema_applied` as the way to tell whether the guarantee held. Verified the True/False split by resolving `supports_json_schema` on all seven provider classes.
- **Category**: docs/completeness
- **Problem**: docs/guides/llm_providers.md:23-30 is the guide's canonical "every provider exposes the same core surface" list. Its `generate_json(...)` bullet (line 26) says only "schema-constrained JSON output where the upstream API supports it", with no mention of the `json_schema=` parameter, of `LLMProvider.supports_json_schema()` (`src/pyutilz/llm/base.py:231`, overridden at `src/pyutilz/llm/openai_compat.py:366`), or of `last_json_schema_applied` — all three introduced by CHANGELOG.md:17 as a headline "[Unreleased] > Added" item whose stated purpose is precisely that a caller must be able to tell whether the guarantee actually held. The neighbouring bullets do carry that level of per-provider detail (line 27 for `get_account_credits`, line 28 for `check_account_limits`, line 30 for `generate_stream`), so the omission is asymmetric.
- **Failure scenario**: A reader of the guide assumes JSON output is equally constrained on every provider, does not call `supports_json_schema()` before a mixed-model sweep, and silently gets prompt-requested JSON rather than schema-enforced JSON on the providers that degrade — exactly the "claiming an unverifiable guarantee" failure the CHANGELOG entry says the API was designed to prevent.
- **Suggested fix**: Extend line 26 with the `json_schema=` / `supports_json_schema()` / `last_json_schema_applied` triple and name which providers return True, matching the per-provider precision of the adjacent bullets.
